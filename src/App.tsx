import { Button } from "@nextui-org/react";

const App = () => {
  return (
    <div className="p-10">
      <Button color="primary" className="bg-blue-500 text-white">
        Button
      </Button>
    </div>
  );
};

export default App;
